export interface User {
  first_name: string;
  last_name: string;
  username:string;
  password:string;
}

export interface Crocodile {
    name: string,
    sex: string,
    date_of_birth: string,
}